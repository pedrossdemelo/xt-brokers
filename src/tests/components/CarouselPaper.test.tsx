import { CarouselPaper } from "components";
import { render } from "tests";
import { papers } from "../mocks";

describe("CarouselPaper component", () => {
  it.todo("implement tests");
  it("", () => {
    render(<CarouselPaper data={papers[0]} />);
  });
});
